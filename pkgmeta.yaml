package-as: Masque_Onyx

required-dependencies:
   - masque

enable-nolib-creation: no

manual-changelog:
  filename: RELEASE.md
  markup-type: markdown

ignore:
  - RELEASE.md
  
tools-used:
  - libstub

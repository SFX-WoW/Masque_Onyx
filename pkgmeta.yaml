package-as: Masque_Onyx

required-dependencies:
  - masque

enable-nolib-creation: no

manual-changelog:
  filename: RELEASE.md
  markup-type: markdown

ignore:
  - _docs\
  - RELEASE.md
  
tools-used:
  - libstub
